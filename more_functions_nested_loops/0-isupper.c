#include <stdio.h>
#include "main.h"

/**
 * main - _isupper check the code.
 *
 * Return: Always 0.
 */

int _isupper(int c)
	{
	if (65 <= c && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	}
