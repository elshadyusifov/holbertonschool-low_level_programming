#include <stdio.h>

/**
 * main - puts command C
 *
 * Description: This program prints a statement about programming,
 * and returns 0 to indicate successful execution.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
